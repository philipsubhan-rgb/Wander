=== line count ===
235 artifacts/trip-planner/src/components/ImageEditor.tsx

=== Wiki refs ===
9:interface WikiResult {
14:async function searchWikiImages(query: string): Promise<WikiResult[]> {
21:    prop: 'pageimages',
27:  const res = await fetch(`https://en.wikipedia.org/w/api.php?${params}`);
57:  const [results, setResults] = useState<WikiResult[]>([]);
67:    const imgs = await searchWikiImages(q);
90:  results: WikiResult[];
157:          <span className="flex items-center gap-1.5"><Search className="h-3.5 w-3.5" />Search Wikipedia</span>

=== structure ===
1:import { useState, useEffect, useRef } from 'react';
2:import { Camera, Search, Link, Trash2, Check } from 'lucide-react';
3:import { Button } from 
5:import { Input } from 
7:import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog';
9:interface WikiResult {
36:function Tab({ active, onClick, children }: { active: boolean; onClick: () => void; children: React.ReactNode }) {
54:function useImagePickerState(searchHint: string) {
89:function SearchGrid({ results, searching, query, setQuery, selected, setSelected, doSearch }: {
136:export interface ImagePickerContentProps {
142:export function ImagePickerContent({ searchHint, onSave, onCancel }: ImagePickerContentProps) {
204:export interface ImageEditorProps {
211:export function ImageEditor({ searchHint, onSave, className = '', size = 'sm' }: ImageEditorProps) {
